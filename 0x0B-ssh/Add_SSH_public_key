# Add the SSH public key

Log in to the server as the ubuntu user.
mkdir -p ~/.ssh.
touch ~/.ssh/authorized_keys.
paste SSH public key into ~/.ssh/authorized_keys file.
Save and close the ~/.ssh/authorized_keys file.
chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys.